apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: internal-app-deployment
  name: internal-app-deployment
  namespace: k8s-apps
spec:
  #replicas: 1
  selector:
    matchLabels:
      app: internal-app
  template:
    metadata:
      labels:
        app: internal-app
    spec:
      containers:
        - name: internal-app
          image: internal-app:0.0.1
          ports:
            - containerPort: 8081